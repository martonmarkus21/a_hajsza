# Szerver címe (a backend alap-URL-je, perjel nélkül a végén).
# Másold át `.env` néven, és állítsd a környezetednek megfelelően (fejlesztés: localhost, éles: https://…).
VITE_API_URL=http://localhost:3000
